const express = require('express'); const cors = require('cors'); const app = express(); const PORT = 3006; app.use(cors({ origin: ['http://localhost:3000', 'http://localhost:3001'], credentials: true })); app.use(express.json()); app.use((req, res, next) => { console.log(\\ - \ \\); next(); }); app.get('/api/orders/teams', (req, res) => { console.log(' GET /api/orders/teams - Buscando todas as equipes'); const teams = [{ id: 'quality-components', name: '? Equipe Verificação de Componentes', description: 'Responsável pela verificação e controle de qualidade de todos os componentes antes da montagem', leader: 'Maria Qualidade', activeProjects: 2, efficiency: 96, completedOrders: 178, tasks: [], members: [{ id: 1, name: 'Maria Qualidade', email: 'maria.qualidade@email.com' }, { id: 2, name: 'Carlos Inspetor', email: 'carlos.qualidade@email.com' }] }, { id: 'assembly-a', name: ' Equipe Montagem A', description: 'Especializada na montagem física das CPUs', leader: 'João Silva', activeProjects: 4, efficiency: 93, completedOrders: 156, tasks: [], members: [{ id: 3, name: 'João Silva', email: 'joao.montagema@email.com' }] }]; res.json(teams); }); app.listen(PORT, () => { console.log(\ MRP2 Backend Server rodando na porta \\); console.log(\ Teams API: http://localhost:\/api/orders/teams\); });
